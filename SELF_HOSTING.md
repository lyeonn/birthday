# 🎂 생일 프로젝트 — 집 서버 셀프호스팅 계획

> 작성일: 2026-06-08
> 목표: Render 무료 Postgres 만료(담달 예정) 대응 + 리눅스/SSH 운영 경험 쌓기

---

## 1. 왜 하는가 (배경 & 결정)

- **마감 있는 문제**: Render 무료 PostgreSQL이 다음 달 만료 → 데이터 날아가기 전에 옮겨야 함
- **부가 목표**: 단순 DB 이전이 아니라, **리눅스 서버 운영 경험**을 이력서용으로 쌓고 싶음
- **결정**: 클라우드(AWS/Neon 등) 대신 **집 데스크탑을 서버로** 써서 직접 운영 경험 쌓기

### 왜 이 선택인가
- "맥에서 돌렸어요"보다 **"리눅스 서버에 배포/운영했어요"** 가 이력서에 훨씬 유리
- 실무 서버는 거의 다 리눅스(우분투). 면접에서 보는 건 SSH·systemd·nginx·방화벽·로그 등 **운영(ops) 경험**
- 데스크탑이 윈도우 → **WSL2**로 윈도우 안에서 진짜 우분투를 돌림 (OS 갈아엎을 필요 없음)

---

## 2. 핵심 개념 정리 (헷갈렸던 것들)

| 용어 | 뜻 |
|---|---|
| **클라이언트** | 사이트에 접속하는 쪽 (크롬/사파리 등 브라우저). 요청하는 쪽 |
| **서버** | 요청받고 응답하는 컴퓨터. 여기선 우리 집 데스크탑 |
| **WSL2** | 윈도우 안에서 진짜 우분투(리눅스)를 돌리는 MS 공식 기능. 가짜 아님, 진짜 리눅스 커널 |
| **SSH** | 멀리 있는 컴퓨터를 터미널로 원격 조종하는 것. 서버 운영의 기본기 |
| **외부 SSH** | 다른 머신으로 네트워크 너머 접속 (예: 맥북 → 데스크탑). 실무 핵심 경험 |
| **Cloudflare Tunnel** | 포트포워딩/고정IP 없이 집 서버를 외부에 안전하게 공개하는 무료 터널 |
| **cloudflared** | Cloudflare Tunnel을 데스크탑에서 돌리는 데몬(프로그램) |

### 자주 한 질문 답변
- **노트북/데스크탑 끄면 DB 날아가나?** → ❌ 안 날아감. 데이터는 디스크에 저장됨. 꺼져 있는 동안 "접속만" 안 될 뿐
- **우분투를 따로 깔아야 하나?** → ❌ 윈도우면 WSL2로 충분. 별도 OS 설치 불필요
- **WSL2가 진짜 리눅스 경험 맞나?** → ✅ SSH로 들어가면 클라우드 우분투 서버랑 명령어·설정 100% 동일
- **맥북→데스크탑 SSH도 진짜 외부 SSH인가?** → ✅ 같은 와이파이여도 다른 머신으로 접속 = 진짜 외부 SSH 경험

---

## 3. 현재 구조 (As-Is) — 클라우드 분산형

```
[클라이언트]
  │ ① 화면 요청
  ▼
[Vercel] ── Next.js 프론트 (birthday-azure-zeta.vercel.app)
  │ ② 데이터 요청 (fetch)
  ▼
[Render] ── NestJS 백엔드 (:3001)
         └─ PostgreSQL DB   ◄── 💀 곧 만료

이미지 업로드 ──► [Cloudinary] (외부 저장소)
```

| 구성요소 | 역할 | 현재 위치 |
|---|---|---|
| Next.js (프론트) | 화면(UI) | Vercel |
| NestJS (백엔드) | 데이터 로직 | Render |
| PostgreSQL (DB) | 데이터 저장 | Render (만료 예정) |
| Cloudinary | 사진 파일 저장 | Cloudinary (→ 데스크탑으로 이전, 폐기 예정) |

---

## 4. 목표 구조 (To-Be) — 집 데스크탑 올인원

```
┌─────────────┐         ┌──────────────────────────────────┐
│   맥북       │  SSH    │   윈도우 데스크탑 (24시간 켜둠)      │
│ (개발/조종)  │ ──────► │   └ WSL2 우분투 (= 리눅스 서버)     │
│ SSH 클라이언트│         │      ├ Next.js 프론트  (:3007)     │
│             │         │      ├ NestJS 백엔드   (:3001)     │
│             │         │      ├ PostgreSQL      (:5432)     │
│             │         │      └ 사진 파일 (uploads/ 디스크)  │
└─────────────┘         └──────────────┬───────────────────┘
                                       │ cloudflared 터널
                                       ▼
                              [Cloudflare] (무료 HTTPS, 집 IP 숨김)
                                       ▼
                              [외부 클라이언트]

   (사진도 데스크탑에 저장 → Cloudinary 더 이상 안 씀)
```

### 역할 분담
- **맥북** = 워크스테이션 (개발 + SSH로 서버 조종)
- **데스크탑 WSL2 우분투** = 리눅스 서버 (프론트+백엔드+DB+**사진 파일** 전부)
- **Cloudflare Tunnel** = 외부에 보이는 유일한 입구 (DB 포트는 인터넷에 노출 안 됨 = 안전)
- **사진 저장** = 데스크탑 `uploads/` 디렉터리에 직접 저장, 백엔드가 `/uploads`로 서빙 (완전 자립)
  - ⚠️ Cloudinary와 달리 자동 백업 없음 → 데스크탑 디스크 고장 대비 **사진도 정기 백업 필요**

### 이 목표가 한 번에 만족하는 것
- ✅ 리눅스 경험 (WSL2 우분투)
- ✅ 외부 SSH 경험 (맥북 → 데스크탑)
- ✅ 집 서버 (데스크탑)
- ✅ DB 만료 마감 해결

---

## 5. 코드/설정상 바뀌는 것 (코드 구조는 그대로)

| 항목 | 지금 | 바뀜 |
|---|---|---|
| 프론트 `NEXT_PUBLIC_API_BASE_URL` | Render 주소 | `https://api.내도메인.com` (또는 임시 터널 주소) |
| 백엔드 `src/main.ts` CORS `origin` | vercel 주소 | 새 프론트 도메인 추가 |
| 백엔드 `.env` `DATABASE_URL` | Render Postgres | `postgresql://...@localhost:5432/birthday` |
| 사진 저장 방식 | Cloudinary | `STORAGE_DRIVER=local` + `PUBLIC_BASE_URL` 설정 → 데스크탑 디스크 저장 |
| 새로 설치 | — | WSL2, PostgreSQL, Node.js, cloudflared |

> 사진 저장은 **env 스위치**로 전환: 설정 안 하면 기존 Cloudinary(현재 Render 안 깨짐), 데스크탑에선 `STORAGE_DRIVER=local`로 디스크 저장. 코드 수정은 `upload.service.ts` + `main.ts`(정적 서빙) 두 곳.

> ⚠️ 코드 리팩토링 없음. **설정값(env) + 실행 위치**만 바뀜.

### 도메인 관련
- **고정 주소 쓰려면 도메인 필요** (예: ~1만원/년) → `내도메인.com` + `api.내도메인.com`
- **연습 단계**: 도메인 없이 임시 무료 터널(trycloudflare)로 먼저 전체 돌려보기 가능 (켤 때마다 주소 랜덤)
- 추천 순서: 임시 터널로 검증 → 잘 되면 도메인 사서 고정

---

## 6. 작업 방식 (부트스트랩 순서)

Claude는 맥북에서 동작 중. 그래서:
- **1~2단계** (WSL2 + SSH 열기): 데스크탑에서 직접 입력 (Claude가 명령어 안내)
- **SSH 뚫린 뒤**: 맥북에서 Claude가 `ssh`로 데스크탑 원격 접속 → 나머지 자동 처리

---

## 7. 로드맵 (할 일)

- [ ] **1. 데스크탑에 WSL2 + 우분투 설치** (이미 있으면 확인)
  - PowerShell에서 `wsl -l -v`로 상태 확인부터
- [ ] **2. 우분투에 SSH 서버 켜기** + 맥북 → 데스크탑 SSH 접속 성공
  - `openssh-server` 설치, 윈도우11 "미러 네트워킹" 모드로 LAN 접속 허용
- [ ] **3. 우분투에 PostgreSQL, Node.js 설치**
- [ ] **4. Render DB → 우분투 Postgres 데이터 이전** (`pg_dump` / `pg_restore`)
- [ ] **5. 코드 서버로 옮기고 백엔드/프론트 실행** (systemd 서비스 등록)
  - 사진 로컬 저장 코드 적용(`upload.service.ts`+`main.ts`) + `.env`에 `STORAGE_DRIVER=local`, `PUBLIC_BASE_URL` 설정
- [ ] **6. Cloudflare Tunnel 연결해서 외부 공개**
- [ ] **7. 데스크탑 항상 켜짐(절전 방지) 설정**

### 추가로 챙길 습관
- **정기 백업**: 데이터가 데스크탑 디스크 한 곳에만 있으니 백업 필수 (디스크 고장 = 데이터 손실 대비)
  - DB → `pg_dump`로 백업
  - **사진 → `uploads/` 폴더도 같이 백업** (Cloudinary 안 쓰므로 자동 백업 없음)

---

## 8. 나중에 (선택) — 이력서 한 단계 더

- 진짜 클라우드 우분투 서버(예: Oracle Cloud 무료 ARM)에 한 번 더 배포해보면
  "원격 클라우드 서버 + 항상 켜짐" 경험까지 채워짐

---

## 9. 아직 안 한 다른 개선 항목 (백로그)

처음 정리했던 할 일들 — DB 이전 끝난 뒤 우선순위대로:
1. 슬립모드 (절전 → 위 7번과 연결)
2. 로딩 느림 / 속도 개선
3. 사진 무한 스크롤
4. AI에 맡긴 코드 정리
5. 로그인 UX 개선 (현재 닉네임+PIN 임시 식별자 → 카카오 로그인 예정)
6. 사진 여러 장 한 번에 업로드
7. 댓글 개수/존재 여부를 목록 페이지에서 확인
8. 전반적 속도
9. **DB 이전 ← 지금 진행 중 (마감 있어 최우선)**
